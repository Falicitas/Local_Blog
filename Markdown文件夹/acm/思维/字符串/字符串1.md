# 字符串1

## 一些习题

### AGC047 B - First Second

每次可以踢掉最前面两个的其中一个。可以发现，一个串$s$能构造的串$t$为串$s$的某个后缀串加上对应前缀串中的一个字符。于是考虑构造Trie树统计构造串的贡献。

但发现如果每个串都根据后缀串+前缀一个字母来构造，空间复杂度会相当高。一个构造串能产生贡献，前提是存在原字符串集存在一个字符串等同于构造串。于是先照后缀构造出一个Trie树；对于每个串，按后缀来生成构造串，如果Trie树存在可以 **放置** 构造串的位置，则查询 + 打标记。复杂度为$O(26n)$，实际上会小很多。