# 数据结构1

## 一些习题

### AGC047 D - Twin Binary Trees

以某个顶点u作为lca的，恰好都是u左子树的叶子发出的路线，然后回到u右子树的叶子。于是暴力枚举每个顶点，然后暴力打上左子树叶子在另一颗树上的路径的标记，然后暴力查询右子树叶子产生的贡献。复杂度为$O(T) = 2O(\frac{T}{2}) + O(T\log T) = O(T\log^2T)$。由于少了一层其实很小。