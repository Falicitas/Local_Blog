# 树

## 一些习题

### CF1436D Bandit in a City

嘛，原本用的二分来AC的：

二分叶节点的容量，然后向上传递容量。如果节点容量<0则退出。

dp的话：$f_u$为以$u$为根节点，叶节点最大的最小容量。记$siz_u$为以u为根的子树的总权值，$leaf_u$为叶节点个数。考虑尽量平摊。于是有$f_u = \max(\max\{f_v\},\lceil\frac{siz_u}{leaf_u}\rceil)$。嘛很好理解。