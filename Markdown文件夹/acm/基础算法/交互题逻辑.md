# 交互题逻辑

## 输入与输出

输出时需要清空缓存区。一般用`fflush(stdout)`。

输入无要求。

结尾可以`return 0`。最后的输出最好清空。

## 调用交互库函数 & 实现交互库函数

需要用到extern "C"声明。可以看「extern C 和头文件」。

如果交互库已经实现了某函数func，则直接在本文件声明`extern "C" int func(int);`。

如果交互库仅声明了函数func，那就要`extern "C" int func(int){...}`。