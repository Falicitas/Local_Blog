# 编程策略

## 善用namespace

namespace本质上是给予了一些函数和变量一个空间，来防止另一些同名函数或同名变量冲突的情况。

一般这样使用：
```cpp
namespace S1
{
    void solve()
    {
        ...
    }
}

int main()
{
    S1::solve();
}
```

倘若不想写前缀，有两种办法：

> using namespace S1.作用域范围内S1的所有变量和函数均不用写前缀。
> using S1::solve. 作用域范围内solve()不用写前缀。





